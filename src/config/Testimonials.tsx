export interface Testimonial {
  name: string;
  username: string;
  body: string;
  profile: string;
}

/**
 * Add real testimonials here, then re-enable <Testimonials /> in
 * src/app/page.tsx. Left empty on purpose until there are genuine ones.
 */
export const testimonials: Testimonial[] = [];
